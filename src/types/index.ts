export interface StreamData {
  user_name: string;
  title: string;
  viewer_count: number;
  // Add other properties as needed
}

export interface ChannelAvatar {
  channel: string;
  url: string;
} 